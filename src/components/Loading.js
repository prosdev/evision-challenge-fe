import React from 'react';

/**
 * A simple loader to display to user when fetching is in progress.
 *
 * @returns {HTML}
 */
const Loading = () => {
    return (
        <div className="loader"></div>
    )
};

export default Loading;